@model JobsApplication.Domain.Dtos.CreateApplicationDto

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>Application</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create" method="post">
            @*<div class="form-control mt-3">
                <label>Candidate</label>
                <select asp-for="CandidateId" class="form-control" asp-items="ViewBag.CandidateId"></select>
            </div>*@
            <input hidden asp-for="CandidateId" value="@(ViewBag.CandidateId)" />
            <div class="form-control mt-3">
                <label>Job Position</label>
                <select asp-for="JobPositionId" class="form-control" asp-items="ViewBag.JobPositionId"></select>
            </div>
            
            <div class="form-group mt-5">
                <input type="submit" value="Create" class="btn btn-primary"/>
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index" asp-controller="Candidates">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
