@model AthletesApplication.Domain.DomainModels.Tournament

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<!-- Create Table for Tournament -->
<!-- Look at the table from any Index page for help -->
<!-- Iterate throught Model.AthletesInTournament -->
<!-- Display: Athlete FullName, Athlete JerseyNumber, Athlete DateOfBirth -->

@* <table class="table">
    <thead>
        <tr>
            <th>Athlete FullName</th>
            <th>Athlete JerseyNumber</th>
            <th>Athlete DateOfBirth</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Athlete.FullName)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Competition.Name)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.DateRegistered)
                </td>
            </tr>
        }
    </tbody>
</table>
 *@

<!--TODO: Display Total Athletes in Tournament-->
<div>
    <p>Total Athletes in Tournament: </p>
</div>